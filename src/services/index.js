import axios from "axios";
import { API_END_POINTS } from "../constants/api-constants";

export function useAPIService() {
  const BASE_URL = "";
  const headers = {
    "Content-Type": "application/json",
    // Authorization: `Bearer ${process.env.REACT_APP_TRADLY_PUBLISHABLE_KEY}`,
  };

  return {};
}
